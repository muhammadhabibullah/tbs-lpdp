Python 3 stdlib only (`re`, `pathlib`, `sys`) for the version-bump utility; the capacity guard described in docs relies on PostgreSQL functions (`pg_database_size`, `pg_class.reltuples`) and Supabase RLS/security-definer RPCs.