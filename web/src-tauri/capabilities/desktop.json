{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "AP-6: in-place application updates through the signed latest.json. Desktop only — tauri-plugin-updater has no Android implementation, so AP-7 falls back to downloading the APK in a browser.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
